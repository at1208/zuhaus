import React from 'react';
import './aboutus.css';
import Header from '../../Common/Header/header'

const AboutUs = ({ match }) => {
  console.log(match)
  return <div>
       <Header />
         </div>
}

export default AboutUs;
